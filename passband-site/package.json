{
  "name": "passband-site",
  "private": true,
  "scripts": {
    "build": "bun build ./main.tsx --outdir ./dist --minify",
    "start": "bun run build && bun run server.ts"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
