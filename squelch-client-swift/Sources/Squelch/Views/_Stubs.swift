// TEMPORARY build stubs — replaced view by view as the port lands.
import SwiftUI

struct AuthView: View { var body: some View { Text("auth") } }
struct RulesView: View { var body: some View { Text("rules") } }
struct AuditView: View { var body: some View { Text("audit") } }
struct UsageView: View { var body: some View { Text("usage") } }
struct SettingsView: View { var body: some View { Text("settings") } }
struct SidePanel: View { var body: some View { Text("side") } }
struct ActionLayer: View { var body: some View { EmptyView() } }
