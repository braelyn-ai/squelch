{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window permissions. All intelligence lives server-side; the shell only needs its own two settings commands (which are always allowed) plus basic window control.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-focus",
    "core:window:allow-show",
    "opener:allow-open-url"
  ]
}
